# Jekyll build output and caches
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# Local bundler install
.bundle/
vendor/

.DS_Store

# Internal build specs — quote unpublished work, keep them out of a public repo
docs/
